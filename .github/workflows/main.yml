name: Andrroid CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    name: Build test app
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out the repository
      uses: actions/checkout@v6

    - name: Run a one line script
      run: echo Hello word!

    - name: Run multi line script
      run: 
        echo Add a action below
        echo test and deploy the app
    
